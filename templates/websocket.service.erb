[Unit]
Description=WebSockets Service
After=network.target

[Service]
Type=simple
User=wsworker
ExecStart=<%= @server['command'] %> <%= @server['port'] %>
Restart=on-abort

[Install]
WantedBy=projects.mount
