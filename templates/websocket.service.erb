[Unit]
Description=WebSockets Service
After=network.target

[Service]
Type=simple
User=wsworker
ExecStart=<%= @config['command'] %> <%= @config['port'] %>
Restart=on-abort

[Install]
WantedBy=projects.mount
